# 使用文档（安装与部署说明）

本项目为二手交易系统，包含前端（Vue 3）与后端（Spring Boot 3 + MyBatis-Plus + MySQL）。本文档面向在 Windows 环境下的本地开发与生产部署，覆盖环境准备、数据库初始化、前后端安装与启动、联调与反向代理、常见问题排查。

- 后端关键文件：[`backend/pom.xml`](backend/pom.xml)、[`application.properties`](backend/src/main/resources/application.properties)
- 前端关键文件：[`frontend/package.json`](frontend/package.json)、[`vue.config.js`](frontend/vue.config.js)、[`src/api/http.js`](frontend/src/api/http.js)
- 数据库结构文档：[`sql/Database.md`](sql/Database.md)
- SQL 文件（建库建表一键执行）：[`sql/Database.sql`](sql/Database.sql)

## 环境要求
- 后端
  - JDK 17（建议 Oracle/OpenJDK 17）
  - Maven 3.9+（用于构建与运行）
  - MySQL 8+（本地默认连接到 `localhost:3306`）
- 前端
  - Node.js LTS（建议 18+）
  - npm（随 Node 安装提供）

## 数据库初始化
2. 直接执行 SQL 文件完成建库与建表  
   该文件包含创建数据库 `secondhand_database` 与所有表结构（含索引与外键）：
   
   ```powershell
   # Windows（PowerShell）
   mysql -u root -p < sql\Database.sql
   ```
若无法使用 SQL 文件，可参考 [`sql/Database.md`](sql/Database.md) 手动执行各表的建表语句。
   
3. 如需启用后台管理面板，给某个用户提权为管理员：
   
   ```sql
   UPDATE users SET role='admin' WHERE username='你的账号';
   ```
   
4. 注意  
   - `products.images` 使用 JSON 类型，需 MySQL 5.7+；本项目推荐 MySQL 8+
   - 如果数据库名或连接地址不同，请同步调整后端的数据库配置（见下文）

## 后端安装与启动
1. 配置数据库连接（两种方式，任选其一）：
   - 直接修改 [`application.properties`](backend/src/main/resources/application.properties)：
     
     ```
     spring.datasource.url=jdbc:mysql://localhost:3306/secondhand_database?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
     spring.datasource.username=root
     spring.datasource.password=123456
     ```
   - 使用环境变量覆盖（当前文件已支持 `DB_USERNAME` 与 `DB_PASSWORD`）：
     
     ```powershell
     $env:DB_USERNAME='root'
     $env:DB_PASSWORD='123456'
     ```

2. 启动服务（默认端口 8080）：
   - 开发模式：
     
    ```powershell
    cd .\backend
    mvn spring-boot:run
    ```
   - 打包并运行：
     
    ```powershell
    cd .\backend
    mvn -q -DskipTests clean package
    java -jar target\SecondHandSystem-0.0.1-SNAPSHOT.jar
    ```

3. 可选：自定义端口（避免冲突）  
   在 [`application.properties`](backend/src/main/resources/application.properties) 增加：
   
   ```
   server.port=8081
   ```

4. 文件上传与静态资源  
   - 上传接口返回形如 `/uploads/<file>` 的相对路径
   - 已将 `/uploads/**` 映射到项目根目录下的 `uploads` 文件夹（自动创建），参见 [`WebConfig.java`](backend/src/main/java/com/lyx/secondhandsystem/config/WebConfig.java)
   - Windows 部署时确保该目录有写权限

## 前端安装与启动
1. 安装依赖：
   
    ```powershell
    cd .\frontend
    npm install
    ```

2. 开发模式启动（默认端口 5173）：
   
   ```powershell
   npm run serve
   ```
   - 开发代理：`/api` 与 `/uploads` 自动转发至 `http://localhost:8080`，参见 [`vue.config.js`](frontend/vue.config.js)
   - 若 5173 端口被占用，可修改 [`vue.config.js`](frontend/vue.config.js) 中的 `devServer.port`
   
3. 构建生产包：
   
   ```powershell
   npm run build
   ```
   构建完成后输出到 `frontend/dist` 目录，可交由任意静态服务器（Nginx/Apache 等）托管。

## 本地联调
- 后端运行在 `http://localhost:8080`
- 前端开发服务器在 `http://localhost:5173`，并通过代理将 `/api` 与 `/uploads` 请求转发到后端
- 前端 HTTP 客户端使用统一基地址 `/api`（见 [`src/api/http.js`](frontend/src/api/http.js)），启用 `withCredentials: true` 携带会话 Cookie

验证示例：
```powershell
# 访问首页与登录页
Start-Process http://localhost:5173

# 测试后端健康（示例使用产品列表）
curl http://localhost:8080/api/products?page=1&size=10
```

## 生产部署（示例：Nginx 反向代理）
目标：前端静态资源与后端接口统一同域，从而携带后端的会话 Cookie（JSESSIONID）。

1. 部署后端：
   - 将生成的 `SecondHandSystem-0.0.1-SNAPSHOT.jar` 与 `application.properties` 放置到服务器
   - 设置数据库环境变量或改写配置
   - 以服务方式启动（示例）：
     
     ```powershell
     java -Xms256m -Xmx512m -jar SecondHandSystem-0.0.1-SNAPSHOT.jar
     ```

2. 部署前端：
   
- 将 `frontend/dist` 发布至 Nginx 静态站点目录
  
3. Nginx 站点配置（关键为统一同域与路径代理）：
   ```nginx
   server {
     listen 80;
     server_name your.domain.com;

     # 前端静态资源
     root   /path/to/frontend/dist;
     index  index.html;

     location / {
       try_files $uri $uri/ /index.html;
     }

     # 代理后端 API（会话基于同域 Cookie）
     location /api/ {
       proxy_pass         http://127.0.0.1:8080/api/;
       proxy_set_header   Host $host;
       proxy_set_header   X-Real-IP $remote_addr;
       proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
       proxy_set_header   X-Forwarded-Proto $scheme;
     }

     # 代理文件上传访问
     location /uploads/ {
       proxy_pass         http://127.0.0.1:8080/uploads/;
       proxy_set_header   Host $host;
     }
   }
   ```
   - 如后端端口不同，请同步调整 `proxy_pass`
   - 若静态文件缓存需控制，可在 `/uploads/` 位置增加 `expires` 或 `add_header Cache-Control`

4. 如不使用同域反向代理（不推荐），需自行将前端的 [`src/api/http.js`](frontend/src/api/http.js) 的 `baseURL` 改为实际后端地址，并为跨域与 Cookie 做额外配置。

## 管理后台登录
- 管理端页面位于前端的“后台管理”入口
- 需使用具备 `role=admin` 的账号登录（见上文“数据库初始化”提权 SQL）
- 管理端路由与守卫位于：[`frontend/src/router/index.js`](frontend/src/router/index.js)

## 常见问题排查
- 数据库连接失败：检查 MySQL 是否启动、账号密码是否正确，或通过环境变量覆盖 `DB_USERNAME/DB_PASSWORD`
- 端口占用：后端默认 8080，可通过 `server.port` 修改；前端默认 5173，可在 `vue.config.js` 修改
- 无法登录或受保护页跳转异常：确认前后端在同域（生产）或使用开发代理（本地）；确保 Cookie（JSESSIONID）被浏览器接收并随请求发送
- 上传失败：检查文件大小（≤5MB）与扩展名（png/jpg/jpeg/webp），确保 `uploads` 目录可写
- 前端构建后访问 404：确认 Nginx 静态站点的 `try_files` 正确回退到 `index.html`（SPA）

## 常用命令速查
```powershell
# 后端（开发/打包/运行）
cd .\backend
mvn spring-boot:run
mvn -q -DskipTests clean package
java -jar target\SecondHandSystem-0.0.1-SNAPSHOT.jar

# 前端（安装/开发/构建）
cd .\frontend
npm install
npm run dev
npm run build

# 数据库（一键导入建库建表）
cd .
mysql -u root -p < sql\Database.sql
```
